import Register from "@/component/register";
import Wrapper from "@/layouts/Wrapper";

export const metadata = {
   title: "Register Onco Network",
};
const index = () => {
   return (
      <Wrapper>
         <Register />
      </Wrapper>
   )
}

export default index